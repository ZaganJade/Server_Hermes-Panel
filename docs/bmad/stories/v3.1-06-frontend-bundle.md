# Story v3.1-06 — Frontend bundling: Alpine npm + Echo + xterm

**Sub-project:** v3.1 Real-time Terminal
**Status:** Done
**Depends on:** v3.1-01 (Reverb env keys for Vite)
**Effort:** M (3–5 hours)
**Branch:** worked directly on `feature/v3.1-realtime-terminal`

## Story

Move Alpine.js from CDN to npm bundle, install xterm + addons + Laravel
Echo + pusher-js, scaffold the JS file layout for the terminal feature,
and verify Echo can connect to Reverb (smoke test only — UI lands in
story 07).

## Acceptance Criteria

1. `package.json` `dependencies` updated:
   - `alpinejs` already present, keep
   - Add `@xterm/xterm`, `@xterm/addon-fit`, `@xterm/addon-search`, `@xterm/addon-web-links`
   - Add `laravel-echo`, `pusher-js`
2. `resources/js/echo.js` initializes Laravel Echo with the `pusher` broadcaster pointed at Reverb, using `import.meta.env.VITE_REVERB_*` values.
3. `resources/js/app.js` imports Alpine, calls `Alpine.start()`, imports `./echo` (so Echo is global), and prepares hooks for terminal module.
4. `resources/js/terminal/` directory created with skeleton files (no UI yet, just exports):
   - `index.js` — exports the Alpine `hermesTerminal()` data factory (to be filled in story 07)
   - `session.js` — exports a class managing WS subscribe + state fetch (skeleton; actual logic in 07)
   - `xterm-instance.js` — exports a factory that builds an xterm + addons instance
   - `history.js` — exports a class with up/down arrow navigation
5. `resources/views/panel/layout.blade.php` removes the `<script src="https://cdn.jsdelivr.net/...alpine.../cdn.min.js">` CDN line. Vite-bundled `app.js` is now the only Alpine source.
6. `npm run build` succeeds. Bundle size logged in commit message — total app.js + chunks ≤ 250 KB gzipped.
7. Manual smoke: open the panel in browser, open dev console, verify `window.Echo` exists and Echo connection to `wss://...` succeeds (status `connected`).
8. Vite hot reload still works for blade changes during `npm run dev`.

## Tasks

- [x] `npm install @xterm/xterm @xterm/addon-fit @xterm/addon-search @xterm/addon-web-links laravel-echo pusher-js`
- [x] Write `resources/js/echo.js`
- [x] Update `resources/js/app.js` (import Alpine, register Alpine, import echo)
- [x] Create `resources/js/terminal/` skeleton with hermesTerminal Alpine factory + TerminalSession + TerminalHistory + createXtermInstance
- [x] Update `layout.blade.php` to drop CDN Alpine and load `app.js` via Vite
- [x] Verify `npm run build` succeeds — app.js 373 KB raw / 104 KB gzipped (under 250 KB target)
- [ ] Manual smoke (Echo connect) — deferred to story 07 once panel is mounted in DOM

## Dev Notes

- Reverb is Pusher-protocol compatible, so `pusher-js` is the right client. Echo config:
  ```js
  window.Echo = new Echo({
    broadcaster: 'pusher',
    key: import.meta.env.VITE_REVERB_APP_KEY,
    wsHost: import.meta.env.VITE_REVERB_HOST,
    wsPort: import.meta.env.VITE_REVERB_PORT ?? 80,
    wssPort: import.meta.env.VITE_REVERB_PORT ?? 443,
    forceTLS: (import.meta.env.VITE_REVERB_SCHEME ?? 'https') === 'https',
    enabledTransports: ['ws', 'wss'],
  });
  ```
- xterm CSS must be imported in `app.css` or `app.js` once: `import '@xterm/xterm/css/xterm.css';`
- For dev local without TLS (`VITE_REVERB_SCHEME=http`), `forceTLS: false` and `wsPort: 8081` direct.

## Smoke Check

1. `npm run build`
2. `docker compose up -d --build`
3. Open panel in browser, open dev console
4. `Echo.connector.pusher.connection.state` → `connected`
5. `Echo.private('terminal.test').listen('.terminal.output', e => console.log(e))` (subscription succeeds, no events yet — that's OK)

## Out of Scope

- Floating panel UI — story 07
- Actually running commands and seeing output — story 07
- E2E tests — story 08

## Definition of Done

See `v3.1-INDEX.md` "Definition of Done".
